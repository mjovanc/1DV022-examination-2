import * as Player from './Player.js'

class Quiz {
  constructor (config) {
    this._config = config
    this._questions = {} // JSON data
    this._players = {} // player objects 
    this._highScores = {}
  }

  // enterNickname () {}

  // getQuestions () {}

  // highScores () {}
}